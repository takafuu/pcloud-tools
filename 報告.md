# 報告

## 現状

- `pcloud-manager` は `help` / `help --ai` / `info` / `status` / `doctor` を備えた public CLI として稼働中。
- pushd は launchd resident + bounded upload executor が稼働中。executor は `--max-records 10`, `PCLOUD_TOOLS_TRANSFER_EXEC_TIMEOUT_SECONDS=3600`。
- diffd は launchd API polling + bounded download executor が稼働中。executor も timeout 3600。
- live queue は clean: pushd planned/missing/manual-review 0、diffd planned/remote/manual-review 0。
- `normal sync/resync`, listing cache operations, autosync launchd changes はこの daemon flow では触らない運用 invariant のまま。

## 完了

- pushd/diffd daemonization は preview-first / human-gated で launchd 登録、resident plist、reload、bounded executor まで完了。
- diffd は account-wide `/diff` 履歴を checkpoint し、以後の新規 remote change を queue/download できる状態。
- upload/download executor は成功時のみ record consume、失敗/timeout/manual-review は保持する方針。
- download suppression / upload origin journal を追加し、local upload echo と download echo を抑制。
- download 中に local same-path が変わった場合は local を保持し、download 済み内容を `*.conflict-YYYYMMDD-HHMMSS.*` に退避する conflict copy を実装・実機検証済み。
- abnormal Discord notify を追加し、同一 `service + kind + path` は既定 3600 秒 dedupe。
- missing local upload records を通常 cleanup surface にし、xbar-safe action と public gated prune を追加。
- `.pcloudmanagerignore` を追加。`!` 行は例外許可ルール。`.env` は除外、`.env.sample` / `.editorconfig` / `.keep` は許可。`.partial` は hard safety exclude のまま。
- `info`, `info paths`, `info config` を追加。`info config` は token と gate env を redacted 表示。

## 実機検証メモ

- Remote -> local file download、remote overwrite、missing local parent download、existing parent download は pass。
- Local -> remote file create/upload と update/upload は pass。
- Hidden/dot temp file と `.partial` は upload されず、resident ingestion で除外。
- Large download conflict test は pass。local edited file を保持し、conflict copy を作成。
- pushd resident は `.pcloudmanagerignore` 導入後に human-gated reload 済み。
- `pcloud-manager help` は `info` を含む root help を表示。`pcloud-manager info paths` は entrypoint/config/state/allowlist/ignore/log を表示。

## 検証

- `tests/test_cli_invariants.py`: 143 passed.
- `scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok.
- `git diff --check`: ok.
- live read-only: `pcloud-manager pushd transfer preview` / `diffd transfer preview` は all zero.

## 残論点

- xbar に `info paths` や `.pcloudmanagerignore` を追加表示する必要はない。CLI で確認すればよい。
- conflict copy を永続 manual-review queue として別管理するかは将来判断。現状は `last-transfer.json` と suppression journal に記録。
- normal sync/resync と listing cache 操作は引き続き別 gate 扱い。

---

## To Implementer Codex: (from Claude reviewer, 2026-05-16)

### 0. まず読むもの

1. `claude_review.md` (worktree commit `685522f`) — pcloud-tools 全体の構造レビュー。7 単位の作業計画と「触らない方が良いもの」の境界。
2. `claude_review_2.md` (worktree 内、未 commit) — 直近 3 commit (`757eff7` / `991e3b8` / `e6bf38f`) のレビュー結果。良かった点 + 1 件の不変条件違反 + 次の単位提案。
3. `todo.md` (`/Users/takafumi/p-core/dev/pcloud-tools/todo.md`, gitignore 運用) — 7 単位それぞれの「対象 / やること / 不変条件 / 受け入れ」テンプレ。本 To Implementer は §5b / §3b / §2b を新規単位として追加する位置付け。

### 1. 検証済み (今回の Codex 作業)

- `pytest -q`: 157 passed (143→157、テスト分割 + 新規 9 件)
- `python3 scripts/pcloud-shadow-validation.py`: `status: ok`
- `python3 -m compileall src`: ok
- 重複 helper 12 関数: `cli_common.py` に完全集約、他モジュールには残存なし
- 新規モジュール: `cli_common.py`, `gates.py`, `io_utils.py`, `cli_service_daemon/launchd_render.py`, `tests/conftest.py`, `tests/test_*.py` 17 ファイル, `tests/test_config.py`
- `download_suppression.py`: `JournalKind` パラメータ化で `_read_journal` / `_write_journal` / `_mark_completed` の単一実装に統合、`atomic_write_json` 経由
- `config.py`: `CONFIG_FIELD_SPECS` テーブル化、`tests/test_config.py` で spec ↔ AppConfig field ↔ `.env.example` の一致を assert

### 2. 1 件だけ次回から直してほしい運用ルール

commit `757eff7` "refactor cli helpers and state writes" の `cli_status.py` 差分 (+316 / -59) に、helper 集約とは無関係な **新規機能** が混入していた:

- `doctor --detail` フラグ追加
- `doctor --plain` フラグ追加
- `daemon_state.read_daemon_state` / `service_daemon_plan.build_pushd_plan` / `build_diffd_plan` / `service_daemon_state.read_service_daemon_state` の利用追加

これは `todo.md §1` の不変条件「argparse の表面、CommandReport の payload、xbar/JSON 出力、exit code、いずれも変わらない」に該当する。テストで動作は固定されているので **revert はしなくてよい**。次回以降、「挙動変更なし」と書いた単位には新規 CLI flag / 新規ロジックを混ぜず、別 commit に分けてほしい(例: `refactor: extract cli_common helpers` と `feat: add doctor --detail/--plain` の 2 commit)。commit message も実態に揃える。

### 3. 次の作業セット (1 PR = 1 単位、上から順番に)

#### §5b: `daemon_state.py` の `diffid` / `auto_download` を atomic_write_text 化

- **対象**: `src/pcloud_tools/daemon_state.py:177` `files["diffid"].write_text(f"{normalized}\n")` と `src/pcloud_tools/daemon_state.py:184` `files["auto_download"].write_text("on\n" if enabled else "off\n")`
- **理由**: launchd `StartInterval=60` の diffd polling と、手動 `pcloud-manager daemon ...` / `pcloud-manager diffd ...` が **同じ 2 ファイル** に書き込みうる並行リスク最大箇所。
- **やること**:
  - `io_utils.py` に `atomic_write_text(path, content)` を追加 (既に内部実装は存在; export を確認)。
  - 上記 2 箇所を `atomic_write_text(files["diffid"], f"{normalized}\n")` 等に置換。
  - `tests/test_state_io.py` の `test_atomic_write_json_preserves_format_and_original_on_replace_failure` をテンプレに、text 版の crash テストを 1 件追加。`diffid` への concurrent write を模擬する必要はない、replace 失敗時に旧内容が残ることだけ確認。
- **不変条件**: ファイル内容(改行込み)が PR 前後で byte-exact。permission / encoding 変えない。
- **受け入れ**:
  - [ ] `pytest -q` 全 pass
  - [ ] shadow validation `status: ok`
  - [ ] 既存 `~/.pcloud/daemon/diffid` を一度読み書きしてもラウンドトリップで内容一致
  - [ ] `grep -nE "write_text" src/pcloud_tools/daemon_state.py` が 0 件

#### §3b: `diffd.launchd.reload` を gates registry に追加 + add_gate_review_args に help を足す

- **対象**: `src/pcloud_tools/gates.py:50` の `GATES` dict に 2 件目の spec を追加。
- **やること**:
  - `GATES["diffd.launchd.reload"]` を追加 (`env_var="PCLOUD_TOOLS_DIFFD_LAUNCHD_RELOAD_GATE"`, `expected_value="operator-approved-diffd-launchd-reload-v1"`, `approval_flags=("--reviewer-approved-bootout-bootstrap", "--reviewer-approved-rollback-policy")`)。
  - `cli_service_daemon/__init__.py` (旧 cli_service_daemon.py) の `_DIFFD_LAUNCHD_RELOAD_GATE_VALUE` 参照箇所を `GATES["diffd.launchd.reload"]` 経由に置換。pushd 側で既に行った手順をミラー。
  - `gates.py:64` `add_gate_review_args` を改修: 各 flag に `help=f"Reviewer approval for {spec.summary} ({flag.lstrip('-')})"` のような短い help を付ける。pushd の `--help` 出力で operator がフラグの意味を読めるようにする。
- **不変条件**: env var 名、期待値、approval flag 名、JSON payload の key/value、いずれも維持。`pcloud-manager-dev diffd launchd reload --help` の表示が増えるだけ。
- **受け入れ**:
  - [ ] tests / shadow validation ok
  - [ ] `pcloud-manager-dev pushd launchd reload --json` / `... diffd launchd reload --json` の出力差分 (generated_at 除く) なし
  - [ ] `pcloud-manager-dev pushd launchd reload --help` の出力に approval flag の help が表示される

#### §2b: `cli_service_daemon/transfer_render.py` を抽出

- **対象**: `cli_service_daemon/__init__.py` (10,335 行) のうち、`pushd transfer ...` / `diffd transfer ...` の **render / print** 関数群。
  - `_render_transfer_check_human`, `_print_transfer_check_report`
  - `_render_transfer_preview_human`, `_print_transfer_preview_report`
  - `_render_transfer_consume_human`, `_print_transfer_consume_report`
  - `_render_validation_matrix_human`, `_print_validation_matrix_report`
  - `_render_real_transfer_run_human`, `_print_real_transfer_run_report`
  - 関連 `_final_real_transfer_review_details`, `_real_gate_approval_details`, `_real_transfer_*_details` も連れていく
- **方針**: launchd_render.py の前例どおり、機械的 move のみ。plist 構築や launchctl 実行はこの単位では触らない。real-run の subprocess は呼ばない。
- **不変条件**: argparse の表面、`pushd|diffd transfer *` の出力(human/JSON/xbar)、exit code 全部維持。
- **受け入れ**:
  - [ ] tests / shadow validation ok
  - [ ] `pcloud-manager-dev pushd transfer preview --json` / `... diffd transfer preview --json` の出力が PR 前後で byte-exact (generated_at 除く)
  - [ ] `wc -l src/pcloud_tools/cli_service_daemon/*.py` で `__init__.py` が目に見えて減っている
  - [ ] `git diff --stat` で挙動を持つコードは move のみ(新規ロジックなし)

### 4. 後続の単位(優先度低、別 To Implementer で切る予定)

- `cli_service_daemon/fswatch_render.py` 抽出 → `api_poll_render.py` → `queue_render.py` → `status_render.py` → `gates_render.py`
- 残り 20 ゲートの gates registry 移植 (`pushd.fswatch.resident` / `diffd.api.long-poll` を次の 2 件にする提案)
- `FieldSpec.default` のテンプレ規約 (`{home}` / `{workspace_root}` / `{{message}}` エスケープ) を `config.py` module docstring に明記
- `tests/test_cli_invariants.py` の 4 行 docstring 残骸を削除
- `cli_common.py` の旧名 alias (`as _action_command` 等) を最終形 (public 名) に書き換え

### 5. 触らない方が良いもの (継続)

- `--reviewer-approved-*` フラグの数自体は維持。減らさない。
- dev-state 限定の `--execute` 仕様。
- `.pcloudmanagerignore` の `!` 例外と `.partial` の hard exclude。
- `mode switch` の terminal gate と `PCLOUD_TOOLS_MODE_SWITCH_GATE`。
- `normal sync/resync` / listing cache / autosync launchd changes を別 gate に置く運用 invariant。

### 6. 完了報告フォーマット

各単位を land したら、本 `報告.md` の `To Reviewer Codex:` セクション (なければ末尾に新設) に以下を書く:

- 完了単位名 (例: `§5b daemon_state.py atomic_write_text 化`)
- commit hash と 1 行説明
- `pytest -q` 結果(件数)
- shadow validation status
- 受け入れチェックボックスの埋まり具合
- 不変条件で気になった点 / 仕様書を更新したか

---

## To Reviewer Codex: Codex implementation report (2026-05-16)

### §5b daemon_state.py atomic_write_text 化

- commit: `fd19ba6 atomically write daemon text state`
- 内容: `write_diffid` / `set_auto_download` を `atomic_write_text` 経由に変更し、text 版の replace failure 保護テストを追加。
- 検証:
  - `python3 -m compileall src`: ok
  - `pytest tests -q`: 158 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - 既存 `~/.pcloud/daemon/diffid` roundtrip: original_equal True
  - `rg -n "\.write_text" src/pcloud_tools/daemon_state.py`: 0 件
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: `diffid` / `auto_download` の file content は改行込みで維持。仕様書更新なし。

### §3b diffd.launchd.reload gates registry 化

- commit: `b62eba6 register diffd launchd reload gate`
- 内容: `GATES["diffd.launchd.reload"]` を追加し、diffd reload validation を pushd と同じ registry 経由へ統一。`add_gate_review_args` に approval flag help を追加。
- 検証:
  - `python3 -m compileall src`: ok
  - targeted: `pytest tests/test_gates.py tests/test_launchd.py -q`: 23 passed
  - `pytest tests -q`: 160 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - `pushd launchd reload --json` / `diffd launchd reload --json`: gate env/expected value 維持を確認
  - `pushd launchd reload --help`: approval flag help 表示を確認
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: env var 名、期待値、approval flag 名は維持。`--help` 表示のみ増加。仕様書更新なし。

### §2b cli_service_daemon/transfer_render.py 抽出

- commit: `85e182e extract transfer render helpers`
- 内容: transfer 系の render / print 関数群を `src/pcloud_tools/cli_service_daemon/transfer_render.py` に移動。`__init__.py` は 10,026 行まで減少。
- 検証:
  - `python3 -m compileall src`: ok
  - targeted: `pytest tests/test_transfer_check.py tests/test_transfer_run.py -q`: 33 passed
  - `pytest tests -q`: 160 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - HEAD snapshot 比較: `pushd transfer preview --json` / `diffd transfer preview --json` は同一 workspace/state で `generated_at` 除外一致
  - `wc -l src/pcloud_tools/cli_service_daemon/*.py`: `__init__.py` 10026, `transfer_render.py` 315, `launchd_render.py` 319
  - `git diff --stat`: `__init__.py` 316 行変更、`transfer_render.py` 315 行追加。render/print の移動のみ。
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: argparse surface、JSON payload、exit code は維持。Claude 指示にあった `_final_real_transfer_review_details` / `_real_gate_approval_details` 等は report construction logic なので、この機械的 render 抽出単位では移動しない判断。仕様書更新なし。

### §5c cli_service_daemon json write atomic 化

- commit: `ad0e0d7 atomically write service daemon json state`
- 内容: `cli_service_daemon/__init__.py` に残っていた `write_text(json.dumps(...))` 7 箇所を `atomic_write_json` 経由に変更。chat notify journal は旧挙動維持のため `ensure_ascii=True` を明示。
- 検証:
  - `python3 -m compileall src`: ok
  - `pytest tests -q`: 160 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - `grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/cli_service_daemon/__init__.py`: 出力なし、0 件
  - `git diff --stat`: `src/pcloud_tools/cli_service_daemon/__init__.py` のみ、15 行変更
- byte-exact 検証:
  - 一時ディレクトリ `/tmp/pcloud-json-byte-exact.F4mxLX` に `old/` と `new/` を作成。
  - `old/` は各対象の旧式 `json.dumps(...)+ "\n"` で書き出し、`new/` は `atomic_write_json` で同じ payload を書き出し。
  - 対象: resident run state、diffd folder cache、long-poll failure state、long-poll run state、checkpoint run state、chat notify journal、last-transfer。
  - `diff -ru old new`: 差分なし。7 件すべて byte-exact。
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: argparse surface、CommandReport payload、xbar/JSON 出力、exit code は変更なし。並行 write を atomic replace にする意図的変更のみ。仕様書更新なし。

### §5d cli_archive / cli_mode / cli_sync json write atomic 化

- commit: `ddc9ada atomically write remaining cli json state`
- 内容: `cli_archive.py` / `cli_mode.py` / `cli_sync.py` に残っていた `write_text(json.dumps(...))` 4 箇所を `atomic_write_json(..., sort_keys=True)` に変更。
- 検証:
  - `python3 -m compileall src`: ok
  - `pytest tests -q`: 160 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - `grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/*.py src/pcloud_tools/cli_service_daemon/*.py`: 出力なし、0 件
  - `git diff --stat`: `cli_archive.py` / `cli_mode.py` / `cli_sync.py` のみ
- byte-exact 検証:
  - 一時ディレクトリ `/tmp/pcloud-json-byte-exact-5d.4RsFHm` に `old/` と `new/` を作成。
  - `old/` は旧式 `json.dumps(..., indent=2, ensure_ascii=False, sort_keys=True)+ "\n"`、`new/` は `atomic_write_json(..., sort_keys=True)` で同じ payload を書き出し。
  - 対象: archive manifest、mode last-switch、autosync launchd last-run、migration last-run。
  - `diff -ru old new`: 差分なし。4 件すべて byte-exact。
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: argparse surface、CommandReport payload、xbar/JSON 出力、exit code は変更なし。並行 write を atomic replace にする意図的変更のみ。仕様書更新なし。

### §2c cli_service_daemon/fswatch_render.py 抽出

- commit: `c8c8602 extract fswatch render helpers`
- 内容: pushd fswatch resident gate/run の human render / print 関数を `src/pcloud_tools/cli_service_daemon/fswatch_render.py` に移動。preview/probe は共通 `print_report` のため対象外。
- 検証:
  - `python3 -m compileall src`: ok
  - targeted: `pytest tests/test_pushd.py -q`: 20 passed
  - `pytest tests -q`: 160 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - HEAD snapshot 比較: `pushd fswatch preview --fixture <tmp> --json` は同一 workspace/state で `generated_at` 除外一致
  - `wc -l src/pcloud_tools/cli_service_daemon/*.py`: `__init__.py` 9912, `fswatch_render.py` 120, `launchd_render.py` 319, `transfer_render.py` 315
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: argparse surface、JSON payload、xbar/JSON 出力、exit code は変更なし。fswatch process 起動や fixture 解析ロジックには未着手。仕様書更新なし。

### §2d cli_service_daemon/api_poll_render.py 抽出

- commit: `5e419f5 extract api poll render helpers`
- 内容: diffd API long-poll gate/run と diffd folder-cache の human render / print 関数を `src/pcloud_tools/cli_service_daemon/api_poll_render.py` に移動。
- 検証:
  - `python3 -m compileall src`: ok
  - targeted: `pytest tests/test_diffd.py -q`: 19 passed
  - `pytest tests -q`: 160 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - HEAD snapshot 比較: `diffd api-poll preview --json` は同一 workspace/state で `generated_at` 除外一致
  - `wc -l src/pcloud_tools/cli_service_daemon/*.py`: `__init__.py` 9761, `api_poll_render.py` 162, `fswatch_render.py` 120, `launchd_render.py` 319, `transfer_render.py` 315
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: argparse surface、JSON payload、xbar/JSON 出力、exit code は変更なし。pCloud API 呼び出しや credentials 処理には未着手。仕様書更新なし。

### §3c gates registry pushd.fswatch.resident / diffd.api.long-poll 追加

- commit: `cd7d961 register resident and api long poll gates`
- 内容: `GATES["pushd.fswatch.resident"]` / `GATES["diffd.api.long-poll"]` を追加し、該当 parser と approval flag 判定、expected value 参照を registry 経由へ変更。
- 検証:
  - `python3 -m compileall src`: ok
  - targeted: `pytest tests/test_gates.py tests/test_pushd.py tests/test_diffd.py -q`: 48 passed
  - `pytest tests -q`: 161 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - HEAD snapshot 比較: `pushd fswatch resident-gate --json` / `diffd api-poll long-poll-gate --json` は同一 workspace/state で `generated_at` 除外一致
  - `pushd fswatch resident-gate --help`: approval flag help 表示を確認
  - `rg -n "PUSHD_FSWATCH_RESIDENT_GATE_VALUE|DIFFD_API_LONG_POLL_GATE_VALUE" src tests`: 0 件
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: env var 名、期待値、approval flag 名、JSON payload、exit code は維持。`add_gate_review_args` による approval flag help 追加のみ。仕様書更新なし。

### §3d gates registry launchd resident plist 追加

- commit: `9c89c0e register launchd resident plist gates`
- 内容: `GATES["pushd.launchd.resident-plist"]` / `GATES["diffd.launchd.long-poll-plist"]` を追加し、resident-plist parser、approval flag 判定、plist gate env/expected value 参照を registry 経由へ変更。
- 検証:
  - `python3 -m compileall src`: ok
  - targeted: `pytest tests/test_gates.py tests/test_launchd.py -q`: 25 passed
  - `pytest tests -q`: 162 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: ok
  - `git diff --check`: ok
  - HEAD snapshot 比較: `pushd launchd resident-plist --json` / `diffd launchd resident-plist --json` は同一 workspace/state で `generated_at` 除外一致
  - `pushd launchd resident-plist --help`: approval flag help 表示を確認
  - `rg -n "LAUNCHD_RESIDENT_PLIST_GATE_VALUE|LAUNCHD_LONG_POLL_PLIST_GATE_VALUE|_pushd_launchd_resident_plist_gate_env|_diffd_launchd_long_poll_plist_gate_env" src tests`: 0 件
- 受け入れ: 全項目 pass。
- 不変条件/仕様書: env var 名、期待値、approval flag 名、JSON payload、exit code は維持。`add_gate_review_args` による approval flag help 追加のみ。仕様書更新なし。

---

## To Reviewer Codex: cleanup tuple minor review notes 完了 (Codex, 2026-05-16)

- commit: `ae91a2b chore: clean up minor review notes`
- 内容:
  - `tests/test_cli_invariants.py` を削除。
  - `FieldSpec` に default template 展開規約の docstring を追加。
  - `chat_notify.py` の bare `except Exception:` に、通知設定失敗で本処理を止めない意図のコメントを追加。
- 検証:
  - `python3 -m compileall src`: ok
  - `./.venv/bin/python -m pytest tests -q`: 162 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: status ok
  - `git diff --check`: ok
  - `ls tests/test_cli_invariants.py`: `No such file or directory`
- 受け入れ: cleanup tuple の対象 3 点は完了。挙動変更なし、仕様書更新なし。

---

## To Reviewer Codex: cli_common 旧名 alias 解消 完了 (Codex, 2026-05-16)

- commit: `f47cdf5 refactor: remove cli_common private aliases`
- 内容:
  - `cli_common` 由来の `as _action_command` / `as _has_errors` / `as _print_report` などの private alias import を public 名 import に変更。
  - 対応する callsite を `_xxx(...)` から `xxx(...)` へ機械的に変更。
  - `cli_status.py` の doctor 用ローカル変数は `has_error_issues` に変更し、public `has_errors` helper と衝突しないように整理。
- 検証:
  - `python3 -m compileall src`: ok
  - `./.venv/bin/python -m pytest tests -q`: 162 passed
  - `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: status ok
  - `git diff --check`: ok
  - `grep -nE "as _action_command|as _has_errors|as _print_report" src/pcloud_tools/*.py src/pcloud_tools/cli_service_daemon/*.py`: 0 件
- 受け入れ: alias cleanup の対象は完了。挙動変更なし、仕様書更新なし。

---

## To Implementer Codex: ack §5c + 残り気になる単位一括ハンドオフ (from Claude reviewer, 2026-05-16)

### Ack: §5c 完了確認

- `pytest -q`: 160 passed
- shadow validation: status ok
- `grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/cli_service_daemon/__init__.py`: 0 件
- chat notify journal は `ensure_ascii=True` 明示済み (L7287)
- byte-exact 検証(`diff -ru old/ new/`)は Codex 側で実施済み、差分なし
- 追加指摘なし、混合 commit の再発もなし。素直に land で OK。

### 全体方針

これまでの §5b/§3b/§2b/§5c と同じく、各単位 1 PR = 1 commit、機械的 refactor 中心。挙動変更なしを徹底。順番は **§5d → §2c → §2d → §3c → §3d → cleanup** を勧めるが、Codex 側で並べ替えても問題ない。各単位を land したら `## To Reviewer Codex:` に同じフォーマットで追記する。

混合 commit 禁止のルールは継続: 新規 CLI フラグ / 新規ロジックは別 commit に分けること。

---

### §5d: cli_archive / cli_mode / cli_sync の raw write_text を atomic_write_json 化

- **対象** (`grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/*.py` で確認):
  - `src/pcloud_tools/cli_archive.py:469` manifest 書き込み
  - `src/pcloud_tools/cli_mode.py:584` mode switch run state
  - `src/pcloud_tools/cli_sync.py:1852` autosync run state
  - `src/pcloud_tools/cli_sync.py:2492` migration run state
- **やること**: 4 箇所すべて `atomic_write_json(path, payload, sort_keys=True)` に 1:1 置換。`path.parent.mkdir(parents=True, exist_ok=True)` は削除可。
- **import 追加**: 各モジュールに `from .io_utils import atomic_write_json` を追加。
- **不変条件**: byte-exact (4 ファイルとも `indent=2, ensure_ascii=False, sort_keys=True` で揃っているので default 引数調整は不要、明示 `sort_keys=True` のみで良い)。
- **受け入れ**:
  - [ ] `pytest -q` 160 passed
  - [ ] shadow validation ok
  - [ ] `grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/*.py src/pcloud_tools/cli_service_daemon/*.py`: 0 件
  - [ ] `git diff --stat`: 上記 3 モジュールのみ

---

### §2c: cli_service_daemon/fswatch_render.py 抽出

- **対象**: `cli_service_daemon/__init__.py` の pushd fswatch 系 **render / print**。
  - `_render_fswatch_resident_gate_human` (L1120)
  - `_print_fswatch_resident_gate_report`
  - `_render_fswatch_resident_run_human` (L1179)
  - `_print_fswatch_resident_run_report`
  - その他 fswatch fixture / probe 系の `_render_*_human` / `_print_*_report` があれば一緒に。
- **方針**: launchd_render.py / transfer_render.py の前例どおり、機械的 move のみ。fswatch process は起動しないし、fixture 解析ロジックも今回は移動しない(別単位)。
- **不変条件**: `pushd fswatch preview/probe/resident-gate/resident-run` の出力 (human/JSON/xbar) と exit code は維持。
- **受け入れ**:
  - [ ] tests / shadow validation ok
  - [ ] `pcloud-manager-dev pushd fswatch preview --json` の出力が PR 前後で byte-exact (generated_at 除く)
  - [ ] `wc -l` で `__init__.py` が目に見えて減る、`fswatch_render.py` 新規

---

### §2d: cli_service_daemon/api_poll_render.py 抽出

- **対象**: `cli_service_daemon/__init__.py` の diffd API poll 系 **render / print**。
  - `_render_api_long_poll_gate_human` (L1234)
  - `_print_api_long_poll_gate_report`
  - `_render_api_long_poll_run_human` (L1297)
  - `_print_api_long_poll_run_report`
  - `_render_diffd_folder_cache_human` (L1349)
  - `_print_diffd_folder_cache_report`
  - その他 api-poll preview の `_render_*` があれば一緒に。
- **方針**: §2c と同じ。pCloud API は呼ばない、credentials 触らない。
- **不変条件**: `diffd api-poll preview/long-poll-gate/long-poll-run`, `diffd folder-cache` の出力と exit code は維持。
- **受け入れ**: §2c と同型。

---

### §3c: gates registry に pushd.fswatch.resident と diffd.api.long-poll を追加

- **対象**: `gates.py` の `GATES` dict に 2 ペア追加。
  - `pushd.fswatch.resident`:
    - env_var: `PCLOUD_TOOLS_PUSHD_FSWATCH_RESIDENT_GATE`
    - expected_value: `operator-approved-fswatch-resident-v1` (現在 `_PUSHD_FSWATCH_RESIDENT_GATE_VALUE`)
    - approval_flags: 現状 `--operator-reviewed-probe`, `--reviewer-approved-queue-policy`, `--reviewer-approved-process-policy` (実コードで使われているフラグセットを `cli_service_daemon/__init__.py` から拾って一致させる)
    - summary: `fswatch resident watcher`
  - `diffd.api.long-poll`:
    - env_var: `PCLOUD_TOOLS_DIFFD_API_LONG_POLL_GATE`
    - expected_value: `operator-approved-api-long-poll-v1`
    - approval_flags: 現状 `--operator-reviewed-preview`, `--reviewer-approved-response-policy`, `--reviewer-approved-credential-policy`, `--reviewer-approved-process-policy` (実コードと一致)
    - summary: `diffd live API long-poll`
- **やること**: `__init__.py` の `_PUSHD_FSWATCH_RESIDENT_GATE_VALUE` / `_DIFFD_API_LONG_POLL_GATE_VALUE` 参照箇所と、対応する `add_argument("--operator-reviewed-...")` 系の手書き定義を `GATES["..."]` 経由 + `add_gate_review_args(parser, GATES["..."])` に置換。pushd reload で既に行った手順をミラー。
- **注意**: approval flag が **正確な集合一致** であること(unknown flag の add は不変条件違反)。`add_gate_review_args` を呼んだ後、parser に同じフラグを手動 add しないこと(重複 add で argparse が壊れる)。
- **不変条件**: env var 名、期待値、approval flag 名、JSON payload、`--help` 表示の文言、いずれも変わらない。`add_gate_review_args` の help 文言は §3b で導入したテンプレに準拠。
- **受け入れ**:
  - [ ] tests / shadow validation ok
  - [ ] `pcloud-manager-dev pushd fswatch resident-gate --json` / `... diffd api-poll long-poll-gate --json` の出力差分 (generated_at 除く) なし
  - [ ] `pcloud-manager-dev pushd fswatch resident-gate --help` で approval flag に help 表示

---

### §3d: gates registry に pushd.launchd.resident-plist と diffd.launchd.long-poll-plist を追加

- **対象**: 同じく 2 ペア追加。launchd plist 系の operational plist write gate。
  - `pushd.launchd.resident-plist`: env_var `PCLOUD_TOOLS_PUSHD_LAUNCHD_RESIDENT_PLIST_GATE`, expected `operator-approved-pushd-launchd-resident-plist-v1`, approval_flags は実コードと一致 (`--reviewer-approved-resident-environment` / `--reviewer-approved-no-bootstrap` あたり、実体確認のうえ)
  - `diffd.launchd.long-poll-plist`: env_var `PCLOUD_TOOLS_DIFFD_LAUNCHD_LONG_POLL_PLIST_GATE`, expected `operator-approved-diffd-launchd-long-poll-plist-v1`, approval_flags 同上
- **やること / 不変条件 / 受け入れ**: §3c と同型。

---

### Cleanup tuple (1 commit でまとめて可)

reviewer 観点で気になる残りの小指摘をひとつの `chore:` commit で処理してよい。

- **tests/test_cli_invariants.py を完全削除**: 4 行 docstring だけ残骸。`tests/conftest.py` が役割を引き継いでいるので削除して問題ない。
- **config.py に FieldSpec default テンプレ規約の docstring**: `CONFIG_FIELD_SPECS` の直前か `FieldSpec` dataclass の docstring に以下を明記:
  ```
  default テンプレ規約:
    - {home}, {workspace_root}, {base_state_dir}, {base_log_dir}, {env_file} は
      _defaults_for_runtime で format 展開される。
    - 展開後に保持したい brace は {{...}} でエスケープ (例: chat_notify_cmd の {{message}})。
  ```
- **chat_notify.py:138 bare `except Exception:` に why コメント**: 通知失敗で本処理を絶対に止めない意図(notify は副作用、コアの動作を阻害しない)を 1 行コメントで明示。
- **cli_common.py 旧名 alias 解消**: 各 cli_* モジュールが `from .cli_common import action_command as _action_command` の形で旧名にエイリアスしている。callsite を `_action_command` → `action_command` (public 名) に書き換え、import の `as _xxx` を削除。**この単位だけは別 commit** にしてほしい (touch するファイルが多いため、上の 3 つの cleanup とは混ぜない)。

#### 受け入れ

- [ ] tests / shadow validation ok
- [ ] `ls tests/test_cli_invariants.py`: 存在しない
- [ ] `grep -nE "as _action_command|as _has_errors|as _print_report" src/pcloud_tools/*.py src/pcloud_tools/cli_service_daemon/*.py`: 0 件
- [ ] `git diff --check`: ok

---

### スコープ外(本ハンドオフでは触らない)

- `cli_sync.py` 内部分割 (autosync / migration 抽出): 代表経路テストの蓄積を様子見してから別単位で。
- gates registry の自動化系 (`automation-gate`, `automation-run`, `real-*`): approval flag が多くてかつ多階層なので、別単位で慎重に。
- `cli_service_daemon/__init__.py` の queue/status/gates 系 render: §2c/§2d の後で別単位。
- `sync_exec.py` の lock/log pointer 系 raw write_text: 並行リスクが構造的に低い (同じ sync lock を取った後に書く)、優先度低。

### 完了報告

毎単位、本 `報告.md` の `## To Reviewer Codex:` セクションに同じフォーマット (完了単位名 / commit hash / 検証結果 / 受け入れチェック / 不変条件メモ) で追記。`報告.md` が長くなったら、解決済み往復は別途 `報告済み.md` に退避してよい (これは reviewer 側で判断する)。

---

## To Implementer Codex: ack + §5c (from Claude reviewer, 2026-05-16)

### Ack: §5b / §3b / §2b 完了確認

- `pytest -q`: 160 passed (Codex 報告と一致)
- `python3 scripts/pcloud-shadow-validation.py`: status ok
- `grep -nE "\.write_text\(" src/pcloud_tools/daemon_state.py`: 0 件
- `pcloud-manager-dev pushd launchd reload --help`: approval flag に `Reviewer approval for launchd bootout/bootstrap reload (...)` 表示確認
- `cli_service_daemon/__init__.py`: 10,335 → 10,021 行 (-314 行 + `transfer_render.py` 315 行)
- 不変条件違反なし、追加指摘なし。3 単位ともそのまま land でよい。
- `pushd.launchd.reload` の summary を `pushd launchd ...` → `launchd ...` に変えた件(2 ゲート共用化)も、subcommand 階層で service が分かるので妥当判断。

### §5c: cli_service_daemon/__init__.py 内の残り 7 箇所を atomic_write_json 化

`grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/cli_service_daemon/__init__.py` で残存する 7 箇所を `io_utils.atomic_write_json` 経由に揃える。

#### 対象

- **L5036** `_write_resident_run_state` (pushd fswatch resident の run state)
  - 元: `state_file.write_text(json.dumps(payload, indent=2, ensure_ascii=False, sort_keys=True) + "\n")`
  - 置換: `atomic_write_json(state_file, payload, sort_keys=True)`
- **L5743** `_write_diffd_folder_cache` (diffd folder cache)
  - 元: `path.write_text(json.dumps(folder_paths, indent=2, ensure_ascii=False, sort_keys=True) + "\n")`
  - 置換: `atomic_write_json(path, folder_paths, sort_keys=True)`
- **L6404** diffd API long-poll の失敗 state 書き込み
  - 置換: `atomic_write_json(state_file, failure_state, sort_keys=True)`
- **L6498** diffd API long-poll の正常 run state 書き込み
  - 置換: `atomic_write_json(state_file, run_state, sort_keys=True)`
- **L6682** diffd API long-poll の checkpoint run state 書き込み
  - 置換: `atomic_write_json(state_file, run_state, sort_keys=True)`
- **L7286** `_write_chat_notify_journal` (chat notify dedupe journal)
  - 元: `path.write_text(json.dumps(journal, indent=2, sort_keys=True) + "\n")`
  - 置換: `atomic_write_json(path, journal, ensure_ascii=True, sort_keys=True)`
  - **注意**: 元コードは `ensure_ascii` を指定せず Python default `True` で書いている。`atomic_write_json` の default は `ensure_ascii=False` なので、byte-exact を守るために **`ensure_ascii=True` を明示**する。
- **L7351** `last-transfer.json` 書き込み
  - 元: `path.write_text(json.dumps(payload, indent=2, ensure_ascii=False) + "\n")`
  - 置換: `atomic_write_json(path, payload)` (default で ensure_ascii=False, sort_keys=False になるので明示不要)

#### やること

- import 追加: `from ..io_utils import atomic_write_json` を `__init__.py` の import セクションへ(既存の `atomic_write_text` 等の隣に並べる想定)。
- 上記 7 箇所を **機械的に 1:1 置換**。
- 各 callsite の `path.parent.mkdir(parents=True, exist_ok=True)` は `atomic_write_json` 側で実施するので削除可能。残しても無害なので判断は Codex に任せる。
- crash テストの追加は **不要**。`tests/test_state_io.py` の json 版が既に `atomic_write_json` の replace 失敗保護を assert 済み。

#### 不変条件

- 7 ファイルの内容(改行込み、`ensure_ascii` の出力差込み、key 順)が PR 前後で byte-exact。
- argparse 表面、CommandReport の payload、xbar/JSON 出力、exit code、いずれも変わらない。
- 並行 write のセマンティクス変化(atomic replace 経由になる)は意図的。それ以外の挙動変更を入れない。

#### 受け入れ

- [ ] `pytest -q` 160 passed(テスト件数維持、新規テスト追加なし)
- [ ] `python3 scripts/pcloud-shadow-validation.py --report-path .dev-state/reports/shadow-validation.json`: `status: ok`
- [ ] `python3 -m compileall src`: ok
- [ ] `grep -nE "\.write_text\(json\.dumps" src/pcloud_tools/cli_service_daemon/__init__.py`: **0 件**
- [ ] `git diff --stat`: 変更は `cli_service_daemon/__init__.py` のみ
- [ ] byte-exact 検証: 一時 state dir で各 file を一度書き出し、PR 前後で `diff` が空

#### スコープ外(本単位では触らない)

- `cli_archive.py:469` / `cli_mode.py:584` / `cli_sync.py:1852, 2492` / `sync_exec.py:*` の raw write_text 系。これらは並行 write リスクが構造的に低いので、別単位で扱う(優先度低)。

#### 完了報告

本 `報告.md` の `To Reviewer Codex:` セクションに以下を追記:
- 完了単位名: `§5c cli_service_daemon json write atomic 化`
- commit hash と 1 行説明
- `pytest -q` 結果、shadow validation status
- `grep` 0 件確認のスクリーンショット相当の出力
- byte-exact 検証の手順と結果

# 引き継ぎ

## 次スレ開始手順

1. 最初に読む: /Users/takafumi/p-core/dev/pcloud-tools/引き継ぎ.md
2. 次に読む: /Users/takafumi/p-core/dev/#仕様書/pcloud-manager/AI向け概要.md
3. 次に読む: /Users/takafumi/p-core/dev/pcloud-tools/todo.md
4. 次に読む: /Users/takafumi/p-core/dev/pcloud-tools/報告.md
5. 必要なら読む: /Users/takafumi/p-core/dev/#仕様書/pcloud-manager/技術仕様.md

## 現在地

- Python 側の既存 `pcloud-manager` 相当 CLI surface は一巡している。
- `src/pcloud_tools/cli.py` は薄い dispatcher になり、command 実装は `cli_action.py`, `cli_daemon.py`, `cli_index.py`, `cli_mount.py`, `cli_status.py`, `cli_sync.py` へ分離済み。
- `status` / `doctor` / `sync` / `sync status` / `mount` / `umount` / `index` / `daemon` / `action` の代表経路は reviewer が確認済み。
- shared report contract は `CommandReport`、versioned JSON、xbar action まで入っている。
- daemon state は `diffid`, auto-download on/off, pending-download, last notification の state/report/preview-first command が入っている。
- pushd/diffd は read-only real-operation gate (`pushd gate`, `diffd gate`) まで追加済み。pushd は fixture-backed fswatch parser (`pushd fswatch preview --fixture <path>`) と one-shot fswatch probe preview (`pushd fswatch probe`)、fswatch 常駐前の read-only checklist (`pushd fswatch resident-gate`) も追加済み。resident-gate は saved shadow validation report、`command -v fswatch`、watch scope、operator probe review、queue policy approval、process lifecycle approval を見るだけで、fswatch は起動せず state writes none。diffd は fixture-backed pCloud diff parser (`diffd diff preview --fixture <path>`) と API poll request-shape preview (`diffd api-poll preview`)、pCloud API long-poll 前の read-only checklist (`diffd api-poll long-poll-gate`) も追加済み。long-poll-gate は saved shadow validation report、preview request shape、diff cursor state、download scope、operator preview review、response policy approval、credential policy approval、process lifecycle approval を見るだけで、API call / cursor mutation / remote-change append は行わず state writes none。`sync autosync-gate` は launchd autosync 変更前の read-only checklist で、saved shadow validation report、`command -v launchctl`、plist presence、enable/disable preview、operator preview review、plist/launchctl/rollback policy approval を見るだけで、launchctl は実行せず state writes none。`sync migration-gate` は normal sync/resync migration validation 前の read-only checklist で、saved shadow validation report、`command -v rclone`、latest sync status、sync lock、document/media scope、normal/resync preview、operator status review、scope/rollback/stop-conditions approval を見るだけで、sync/resync は実行せず state writes none。diffd planned downloads には allowlist/default-exclude policy を適用済み。transfer は `pushd transfer preview` / `diffd transfer preview` で `rclone copyto` argv を出し、`pushd transfer check` / `diffd transfer check` は real transfer gate 前の read-only checklist を出す。`transfer check` は `--confirm-path` / `--confirm-direction` / `--consume-policy` / `--timeout-policy` で first real run の operator/reviewer confirmation を read-only に記録でき、`--final-review` で ready 時だけ display-only の dry-run command と exact real command を表示できる。blocked 時は不足 check を列挙し、transfer command strings は出さない。ready 時も `ready-for-separate-gate` であり、実 transfer execution が使える意味ではない。`pushd transfer real-gate` / `diffd transfer real-gate` は別 real execution gate の read-only scaffold で、`PCLOUD_TOOLS_REAL_TRANSFER_EXECUTION_GATE=operator-approved-real-transfer-v1` を要求し、fake-rclone gate reuse forbidden を返す。approval flags が揃うと `complete-read-only` になるが real-gate 自体は実行しない。operator verification required は read-only report に表示され、通常の read-only diagnostics は自動検証だけでよく、人間確認は first real target / real execution gate / actual pCloud-rclone transfer に寄せる。human gate status も機械判定でき、service gate は `required-before-real-work`、real-gate approval complete は `required-before-actual-transfer` を返す。future real-run policy は read-only に文書化済みで、成功時だけ該当 record を消費し、失敗/不明時は保持し、自動 rollback/delete はしない。`pushd transfer real-run --execute` / `diffd transfer real-run --execute` は guarded real execution path になり、final-review 引数、approval flags、専用 real gate env、non-`fake-rclone` の `PCLOUD_TOOLS_RCLONE_BIN`、`--execute` が揃うまで error / state writes none。成功時は `last-transfer.json` に mode `real-rclone-transfer` を記録するが queue/change record は自動消費しない。`pushd transfer run --execute` / `diffd transfer run --execute` は dev-state + `PCLOUD_TOOLS_TRANSFER_EXECUTION_GATE=dev-fake-rclone` + `.dev-state/.../fake-rclone` のテスト実行だけ許可する。fake-rclone 実行には `PCLOUD_TOOLS_TRANSFER_EXEC_TIMEOUT_SECONDS` の timeout が入り、timeout 時は fake process group cleanup を試み、成功/失敗は `last-transfer.json` に記録する。fswatch 常駐起動、pCloud API long-poll 起動、追加の実 pCloud upload/download、launchd/autosync execution、normal sync/resync validation execution はまだ開いていない。
- `todo.md` 上の完了済み:
  - shared status/action contract
  - major command surface port
  - sync fallback command flows
  - mount/umount/index
  - daemon state
  - pushd/diffd read-only real-operation gate
  - pushd fswatch fixture parser
  - pushd one-shot fswatch probe preview
  - diffd pCloud diff fixture parser
  - diffd one-shot API poll preview
  - upload/download executor previews
  - diffd download allowlist/default-exclude policy
  - dev fake-rclone guarded transfer execution path
  - fake-rclone transfer timeout/process-group cleanup
  - pushd/diffd status last-transfer summary
  - real-transfer gate read-only checklist commands
  - read-only first-real-run target/policy confirmation flags
  - read-only final-review dry-run display
  - blocked final-review human output without transfer command strings
  - ready final-review handoff marked as separate-gate only
  - read-only separate real execution gate scaffold
  - hard refusal real-run surface
  - read-only real-gate approval flags
  - explicit real-run refusal for ignored gate env values
  - current pushd/diffd gate next-unit guidance
  - read-only future real-run consume/rollback policy
  - operator verification requirement marker for transfer gate output
  - operator verification marker for service gate diagnostics
  - stable transfer consume preview action ids
  - concise shadow validation summary output
  - machine-readable real execution readiness markers
  - real-run refusal readiness markers
  - transfer preview/fake-rclone run real-execution blocked markers
  - transfer-check real-execution blocked markers
  - transfer consume real-execution blocked markers
  - human-gate blockers for remaining real-work items
  - guarded real-run execution path with stub-rclone test coverage
  - read-only fswatch resident gate checklist
  - guarded foreground fswatch resident-run with bounded fake-fswatch validation
  - read-only pCloud API long-poll gate checklist
  - read-only autosync launchd gate checklist
  - read-only normal sync/resync migration gate checklist
  - thin dev wrappers for `pcloud-pushd` / `pcloud-diffd`
  - dev-state-only autosync plist preview/write scaffold
  - read-only old monolith archive gate checklist
  - concise aggregate `gates status`
- public `pcloud-manager` entrypoint は Python 実装へ切替済み。
- 2026-05-04 に first real pushd/upload target `Documents/DQ2-呪文.png` を人間確認後に実行済み。`pushd transfer real-run --execute` は `/usr/local/bin/rclone copyto ... pcloud:core/Documents/DQ2-呪文.png` を起動し、returncode 0、timeout なし。`last-transfer.json` は `mode: real-rclone-transfer`、queue record は承認済み `remove-on-success-retain-on-failure` policy に従って消費済み。`pushd transfer preview` は planned transfers 0。
- 2026-05-04 に first real diffd/download target `Documents/DQ2-呪文.png` も人間確認後に実行済み。download 前に `.dev-state/real-transfer-backups/DQ2-呪文.png.before-download` へ退避し、`diffd transfer real-run --execute` は `/usr/local/bin/rclone copyto pcloud:core/Documents/DQ2-呪文.png .../Documents/DQ2-呪文.png` を起動、returncode 0、timeout なし。download 後 SHA-256 は退避ファイルと同じ `c0412cf18081b35bee90f0fd30dfd6c0d0d0a0c8a10c0f362b326de2c090cccb`。remote-change record は承認済み policy に従って消費済み。`diffd transfer preview` は planned transfers 0。
- 上記により first real upload/download limited migration validation は完了扱い。fswatch resident、pCloud API long-poll、autosync launchd change、normal sync/resync migration validation は read-only checklist だけ追加済みで、実 start/execution は未実装。残 gate は fswatch resident start、pCloud API long-poll start、autosync launchd execution、normal sync/resync migration validation execution、old monolith archive。ここから先は常駐/自動化/同期全体に近いので別途 human gate を置く。
- `sync autosync-plist` は dev `.dev-state/com.example.pcloud-bisync.dev.plist` の preview/write scaffold。`--execute` でも dev-state plist だけを書き、launchctl / scheduled sync / normal sync は実行しない。
- `sync migration-gate --sync-status-report-path <sync-status.json>` は保存済み `pcloud-manager sync status --json` report を read-only 入力として使える。dev workspace の古い sample `bisync_status.log` ではなく、保存 report の latest result / lock / scope を preflight に使うだけで、sync/resync は実行しない。`gates status` も同じ引数を渡せる。
- `gates status` は各 gate の approval/blocker だけでなく、対応する guarded `*-run` command、専用 execution env gate、run scope も read-only で表示する。`--show-command-examples` を付けると read-only review command 例も出すが、例には `--execute` を含めない。表示だけで実行 gate は開かない。
- `pushd fswatch resident-run` は foreground resident loop の guarded 実行経路。default は preview/refusal で、`--execute` は resident approvals と `PCLOUD_TOOLS_PUSHD_FSWATCH_RESIDENT_GATE=operator-approved-fswatch-resident-v1` が揃うまで拒否する。実行時は fswatch event を pushd queue record に変換して `fswatch-resident-last-run.json` を記録するが、upload transfer / launchd / sync-resync / pCloud API は実行しない。shadow validation では temp fake-fswatch + `--max-events 1` だけ実行済み。
- 2026-05-04 に human approval 後、実 `/opt/homebrew/bin/fswatch` で `pushd fswatch resident-run --execute --max-events 1` を bounded 実行済み。`.dev-state` probe は excluded event 1 件で正常終了、`Documents/fswatch-resident-live-probe.txt` probe は queue record 1 件 append を確認後、`pushd queue remove ... --execute` と probe file 削除で cleanup 済み。最終確認は `pushd transfer preview` planned transfers 0、`pushd status` queue length 0。常駐し続ける resident や launchd 登録は未実行。
- `diffd api-poll long-poll-run` は guarded API long-poll 実行経路。default は preview/refusal。fixture 実行は `--fixture`、live API 実行は `--live-api` が必要で、どちらも API approvals、saved shadow report、`PCLOUD_TOOLS_DIFFD_API_LONG_POLL_GATE=operator-approved-api-long-poll-v1`、`--execute` が揃うまで副作用なし。credential は `PCLOUD_TOOLS_PCLOUD_API_TOKEN` 優先、未設定なら `PCLOUD_TOOLS_CORE_REMOTE` から推定した rclone pCloud remote (`RCLONE_CONFIG` or `~/.config/rclone/rclone.conf`) の OAuth `token.access_token` を `access_token` parameter として使う。`pcloud-crypt` の `password` / `password2` は読まない。report/log では token を redact する。実行時は allowlist を通った download record を `diffd/remote-changes.json` に append、numeric diffid を daemon state に書き、`api-long-poll-last-run.json` を記録する。download transfer / launchd / sync-resync は実行しない。shadow validation では fixture、fake local HTTP live-API、temp rclone config fallback のみ実行済み。
- 2026-05-04 に human approval 後、実 pCloud `/diff` one-shot を `--live-api --max-iterations 1 --execute` で複数回実行済み。rclone config fallback credential を使い、token は redacted。1回目は `current diffid: 0`, `new diffid: 100`, `invalid changes: 47`。その後 parser を pCloud metadata response 形状に合わせ、`reset` / folder / userinfo は skip、file は direct `path` または同一 batch 内 folder metadata の `parentfolderid` から path 復元するよう修正済み。
- 2回目の実 pCloud `/diff` one-shot は `current diffid: 100`, `new diffid: 200`, `invalid changes: 53`。3回目は `current diffid: 200`, `new diffid: 300`, `invalid changes: 52`, `folder cache: 0 -> 0`。全件 `parentfolderid=29913863697` 配下で、親 folder は初回 batch の top-level `bench_test` だったため、batch をまたぐ folderid-to-path cache が必要と判明。`diffd/folder-cache.json` を追加し、folder metadata を cursor state と同じく保持して次 batch の path 復元に使うよう修正済み。
- `diffd folder-cache status|add|remove|clear` を追加済み。preview-first で、`--execute` がある場合だけ dev-state の `.dev-state/state/diffd/folder-cache.json` を更新する。実機 dev-state には、観測済み live `/diff` response に基づき `29913863697 -> bench_test` を seed 済み。`diffd api-poll long-poll-run --live-api --max-iterations 1` の read-only preview は `current diffid: 300`, `folder cache: 1 -> 1`, `state writes: none`。次の live one-shot では `bench_test` 配下イベントは path 復元後、allowlist/default-exclude により skipped になる見込み。
- folder-cache seed 後の live pCloud `/diff` one-shot は `current diffid: 300`, `new diffid: 400`, `parsed changes: 51`, `download records appended: 0`, `skipped records: 51`, `invalid changes: 0`, `folder cache: 1 -> 1`。`diffd preview` は `daemon diffid: 400`, remote changes/pending downloads/planned downloads すべて 0。live API parser/folder-cache は実機で期待通り動いた。download transfer / launchd / sync-resync は未実行。
- `sync autosync-run enable|disable` は launchd change の guarded 実行経路。default は preview/refusal で、`--execute` は autosync approvals、saved shadow report、launchctl/plist checks、`PCLOUD_TOOLS_AUTOSYNC_LAUNCHD_GATE=operator-approved-autosync-launchd-v1` が揃うまで拒否する。実行時は planned launchctl commands だけを実行して `sync/autosync-launchd-last-run.json` を記録する。normal sync/resync は直接実行しない。shadow validation では fake-launchctl のみ実行済み。
- 2026-05-05 に human approval 後、実 `/bin/launchctl` で `sync autosync-run enable --execute` と `sync autosync-run disable --execute` を実行済み。disable 後は dev autosync state `not_loaded`、sync lock missing、gates status は all execution gates closed。実 normal sync/resync は実行していない。
- `sync migration-run normal|resync` は normal/resync migration validation の guarded 実行経路。default は preview/refusal で、`--execute` は migration approvals、saved shadow report、saved sync status report、rclone check、`PCLOUD_TOOLS_SYNC_MIGRATION_GATE=operator-approved-sync-migration-v1` が揃うまで拒否する。実行時は explicitly approved `rclone bisync` command だけを実行し、sync log/status と `sync/migration-last-run.json` を記録する。launchd / listing cache 削除移動 / fswatch / pCloud API は実行しない。shadow validation では fake-rclone のみ実行済み。
- 2026-05-05 時点で `sync migration-run normal` と `sync migration-run resync` の read-only readiness はどちらも `sync/resync can run: yes`。planned normal command は `/usr/local/bin/rclone bisync /Users/takafumi/p-core/dev/pcloud-tools pcloud:core --conflict-resolve newer --resilient --skip-links --filter-from .../bisync.filter --log-file .../bisync-normal-<timestamp>.log --log-level INFO`。planned resync command は同じ bisync に `--resync-mode path1` が付く。次に実行するなら normal だけを first validation とし、resync は rollback/repair gate 扱いにする。
- 2026-05-05 の実 `sync migration-run normal --execute` は rclone bisync prior lock で exit=1。lock は `/Users/takafumi/Library/Caches/rclone/bisync/local__Users_takafumi_p-core_dev_pcloud-tools..pcloud_core.lck`、PID `66607` は実行中でない。CLI はこの rclone cache lock を preflight で検知して `sync/resync can run: no` にするよう修正済み。cleanup candidate は `rclone deletefile /Users/takafumi/Library/Caches/rclone/bisync/local__Users_takafumi_p-core_dev_pcloud-tools..pcloud_core.lck`。まだ削除していない。
- 旧 `com.takafumi.pcloud-bisync` LaunchAgent は loaded だが `state = not running`, `run interval = 300 seconds`。`ps` では rclone/bisync/pcloud-manager 実行中プロセスなし。stale lock cleanup と normal sync retry の前に、この旧 watchdog を止める/残す判断が必要。
- その後 human が旧 `com.takafumi.pcloud-bisync` を `launchctl bootout/disable` し、stale rclone lock を `rclone deletefile` で削除済み。`./pcloud-manager-dev` での retry は exit=7 で prior listings 不在。ただし planned command が `/Users/takafumi/p-core/dev/pcloud-tools pcloud:core` になっており、これは保存済み status の `/Users/takafumi/p-core` と root mismatch。CLI は saved status allowlist root と config core_dir が違う場合に `migration target root: mismatch` / `sync/resync can run: no` で止めるよう修正済み。
- 正しい normal migration retry は public `pcloud-manager` 側で行う。read-only preview 済み: `PCLOUD_TOOLS_SYNC_MIGRATION_GATE=operator-approved-sync-migration-v1 pcloud-manager sync migration-run normal --report-path /Users/takafumi/p-core/dev/pcloud-tools/.dev-state/reports/shadow-validation.json --sync-status-report-path /Users/takafumi/p-core/dev/pcloud-tools/.dev-state/reports/sync-status.json --operator-reviewed-status --reviewer-approved-scope --reviewer-approved-rollback-policy --reviewer-approved-stop-conditions` は `target root: ok (/Users/takafumi/p-core)`, `sync/resync can run: yes`, planned command `/usr/local/bin/rclone bisync /Users/takafumi/p-core pcloud:core ...`。
- human approval 後、public `pcloud-manager sync migration-run normal --execute` は成功済み。rclone log `/Users/takafumi/.pcloud/logs/bisync-normal-20260505-004947.log` は `No changes found` / `Bisync successful` / `Transferred: 0 B`。`pcloud-manager sync status --json` は `sync state: synced`, `last result: 2026-05-05 00:52:17 SUCCESS mode=normal`, `last error status: historical`, `autosync state: not_loaded`。public autosync enable の read-only preview は ready だが、`--execute` はまだ実行していない。
- human approval 後、public `pcloud-manager sync autosync-run enable --execute` も成功済み。runatload の初回 autosync log `/Users/takafumi/.pcloud/logs/bisync-autosync-20260505-013703.log` は `No changes found` / `Bisync successful` / `Transferred: 0 B`。`pcloud-manager sync status` は `sync state: synced`, `last result: 2026-05-05 01:39:41 SUCCESS mode=autosync`, `last error status: historical`, `sync lock status: missing`, `autosync state: active`, `autosync runs: 1`。`launchctl print gui/501/com.takafumi.pcloud-bisync` は loaded, `state = not running`, `last exit code = 0`, `run interval = 300 seconds`。
- その後の autosync も成功し、`pcloud-manager sync status` は `last result: 2026-05-05 02:02:26 SUCCESS mode=autosync`, `autosync runs: 4`, `sync lock status: missing`。
- `archive old-monolith-run` は old zsh `pcloud-manager` monolith backup の guarded archive 実行経路。default は preview/refusal で、`--execute` は archive approvals と `PCLOUD_TOOLS_OLD_MONOLITH_ARCHIVE_GATE=operator-approved-old-monolith-archive-v1` が揃うまで拒否する。実行時は selected cutover backup の `pcloud-manager.current` と `shadow-validation.json` を `.dev-state/old-monolith-archive/<backup>/` にコピーし、`archive-manifest.json` を記録する。source backup は残し、public wrapper / launchd / sync state / remote は触らない。shadow validation では temp backup だけ実行済み。
- human approval 後、実 cutover backup `.dev-state/cutover-backups/20260426-040551` を使って `archive old-monolith-run --execute` 成功済み。archive target は `.dev-state/old-monolith-archive/20260426-040551` で、`pcloud-manager.current`, `shadow-validation.json`, `archive-manifest.json` がある。manifest は public wrapper / launchd / sync を変更していないことを記録。old monolith delete はまだしていない。
- 最終 human 確認済み: `pcloud-manager sync status` は `sync state: synced`, `last result: 2026-05-05 02:02:26 SUCCESS mode=autosync`, `last error status: historical`, `sync lock status: missing`, `autosync state: active`, `autosync runs: 4`。shadow validation は `155 ok; 0 failed`。public `pcloud-manager` は Python wrapper として稼働中、document/media allowlist autosync は復帰済み、old monolith は archive target に保全済み。
- live sync scope は document-only allowlist (`Documents/`, `scansnap/`, `screenshots/`, `sound/`) へ整理済み。
- 2026-04-27 に document-only bisync baseline recovery、manual normal sync、autosync 復帰まで reviewer 通過済み。latest autosync は `SUCCESS mode=autosync`、autosync `active`、lock `missing`、scope entries `4`。

## 重要判断

- 仕様の正本は `/Users/takafumi/p-core/dev/#仕様書/pcloud-manager/技術仕様.md`。AI 再開用の短縮版は `/Users/takafumi/p-core/dev/#仕様書/pcloud-manager/AI向け概要.md`。
- config は `.env` 集約で `/Users/takafumi/p-core/dev/pcloud-tools/src/pcloud_tools/config.py` を正本にする。各 command から raw env を散らして読まない。
- dev mode では live state を壊さない。`pcloud-manager-dev` からの sync/mount/background/autosync 危険経路は preview-first / execute refusal を維持する。
- `sync` はサブシステム扱い。document-only allowlist と autosync 復帰状態を崩さない。
- reviewer との往復は `/Users/takafumi/p-core/dev/pcloud-tools/報告.md` を正本にする。
- 全実装が本当に終わったら「全部実装終わりです」と明示する。未完了が残る間は次の作業単位を切って返す。

## 未完了

- `pcloud-pushd` / `pcloud-diffd` の resident/long-poll 実装。
- real shadow/limited migration validation。
- old monolith archive execution。
- pushd/diffd の fswatch resident start / pCloud API polling / 追加の real rclone/pCloud transfer validation。
- 古い launchd `/tmp/pcloud-bisync.out` / `.err` を運用メモのままにするか整理するかの判断。

## real transfer gate 条件

real rclone/pCloud upload-download はまだ開けない。次に開く場合も、少なくとも以下を満たしてから別 gate として扱う。

- dev fake-rclone path の現状確認が通ること: `python3 -m compileall src`, `./.venv/bin/python -m pytest tests/test_cli_invariants.py -q`, `python3 scripts/pcloud-shadow-validation.py --json`, `git diff --check`
- saved shadow validation report が `status: ok` で、temp workspace / temp state dir guard が通っていること
- queue / remote-change をいつ消費するか、失敗時に残すか戻すかを reviewer が承認していること
- first real run の対象 path と方向を operator が明示していること
- real rclone command preview が operator に提示され、想定 remote/local path と一致していること
- timeout / process group cleanup / last-transfer status 表示の挙動を real execution でもどう扱うか決めていること
- pCloud API long-poll、fswatch 常駐、launchd 変更、old monolith archive と同時に進めないこと

## 地雷

- 次の大物は `pcloud-pushd` / `pcloud-diffd`。fswatch 常駐、pCloud API long-poll、実 rclone/pCloud upload/download、launchd、public entrypoint 切替を一気にやらない。
- 次も非破壊 scaffold の延長に切る。dev mode では `.dev-state/` 配下だけを読む・書き、real-operation gate を開かない。
- `Documents/` は repo 直下に未追跡で見えるが既存ユーザーデータ。触らない。
- `報告.md` は小さく保つ。肥大化したら解決済み往復を `報告済み.md` へ退避する。
- allowlist/filter rollback backup は `/Users/takafumi/.pcloud/allowlist-backups/20260426-052756/`。
- bisync listing rollback backup は `/Users/takafumi/.pcloud/bisync-listing-backups/20260427-220757/`。
- `last error` や `/tmp/pcloud-bisync.out` / `.err` は autosync 復帰途中の失敗が過去記録として残る場合がある。latest result を優先して判断する。
- 過去 reviewer 指摘:
  - test env は `HOME` / `XDG_CACHE_HOME` を tmp 配下に固定する。
  - conflict/orig/rej/backup 系の不要ファイルを残さない。
  - config error 時に `--execute` 系が副作用を出さない。

## 次の一手

- 別スレへの初回指示は以下をそのまま使う:

  ```text
  /Users/takafumi/p-core/dev/pcloud-tools/引き継ぎ.md を最初に読んで。
  その後 todo.md を見て、pcloud-pushd / pcloud-diffd の残作業を小さな実装単位に分解して進めて。
  Coder/Reviewer は交互運用。Coder が実装や報告を書いたら Reviewer が確認し、Reviewer が次指示を書いたら Coder が進める。
  pCloud sync は現時点で document/media allowlist autosync が復帰済みなので、sync 復旧作業を勝手に再実行しない。
  まずは dev-state / preview-first / read-only 確認で進め、fswatch 常駐、pCloud API long-poll、実 upload/download、launchd 変更、old monolith archive は専用 gate を作ってから進める。
  ```

- 自動開発で進めてよい範囲:
  - `todo.md` の pushd/diffd 残作業を小さく切ること。
  - dev-state 配下だけを使う preview/status/plan/report/test の追加。
  - unit test / compileall / shadow validation / `git diff --check`。
  - docs と `報告.md` / `報告済み.md` の整理。
- 自動開発でも慎重 gate が必要な範囲:
  - real fswatch 常駐や launchd 変更。
  - pCloud API long-poll。
  - 実 upload/download。
  - retained old monolith backup/source files deletion.
  - autosync restart/load 変更、manual normal sync、resync、`--force`、listing cache 操作。
- まず `/Users/takafumi/p-core/dev/pcloud-tools/報告.md` の `To Implementer Codex:` を読む。
- 次の実装単位は、ユーザーが再開指示した範囲に合わせる。dev fake-rclone execution path は入ったが、実 upload/download は別 gate が必要。gate なしで続けるなら `last error` 表示改善など同期操作を伴わない別タスクに切る。
- pushd/diffd を進める場合は、real fswatch / pCloud API polling / transfer execution を一気に入れず、preview-first、dev-state guard、`pushd gate` / `diffd gate` の closed 表示を維持する。
- この単位でやらないこと:
  - normal sync 手動実行
  - resync 再実行
  - `--force`
  - listing cache の削除/移動
  - autosync restart/load 変更
  - launchd 変更
  - fswatch 常駐
  - pCloud API long-poll
  - retained old monolith backup/source files deletion
- 確認コマンド:
  - `git status --short`
  - `python3 -m compileall src`
  - `./.venv/bin/python -m pytest tests/test_cli_invariants.py -q`
  - `git diff --check`
  - 追加した pushd/diffd status/preview command
  - `./pcloud-manager-dev pushd gate --json`
  - `./pcloud-manager-dev diffd gate --json`
  - `./pcloud-manager-dev sync autosync-gate --json`
  - `./pcloud-manager-dev sync migration-gate --json`
  - `./pcloud-manager-dev pushd fswatch preview --fixture <fixture> --json`
  - `./pcloud-manager-dev pushd fswatch probe --json`
  - `./pcloud-manager-dev diffd diff preview --fixture <fixture> --json`
  - `./pcloud-manager-dev diffd api-poll preview --json`
  - `./pcloud-manager-dev diffd api-poll long-poll-gate --json`
  - `./pcloud-manager-dev diffd api-poll long-poll-run --json`
  - `./pcloud-manager-dev sync autosync-run enable --json`
  - `./pcloud-manager-dev sync migration-run normal --json`
  - `./pcloud-manager-dev archive old-monolith-run --json`
  - `./pcloud-manager-dev pushd transfer preview --json`
  - `./pcloud-manager-dev pushd transfer check --json`
  - `./pcloud-manager-dev diffd transfer preview --json`
  - `./pcloud-manager-dev diffd transfer check --json`
  - fake-rclone を `.dev-state/` 配下に置いた上で `PCLOUD_TOOLS_TRANSFER_EXECUTION_GATE=dev-fake-rclone` / `PCLOUD_TOOLS_RCLONE_BIN=<fake-rclone>` を設定し、`./pcloud-manager-dev pushd transfer run --execute --json` と `./pcloud-manager-dev diffd transfer run --execute --json`
  - 追加した action id
  - `./pcloud-manager-dev status --json`
  - `./pcloud-manager-dev doctor --json`
  - `./pcloud-manager-dev daemon status --json`
  - `./pcloud-manager-dev action status.refresh`

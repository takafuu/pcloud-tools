# 引き継ぎ

## 最初に読むもの

- `/Users/takafumi/p-core/dev/pcloud-tools/thread-status.md`
- `/Users/takafumi/p-core/dev/pcloud-tools/報告.md`
- `/Users/takafumi/p-core/dev/#仕様書/pcloud-manager/AI向け概要.md`
- 必要なら `/Users/takafumi/p-core/dev/pcloud-tools/for-xbar.md`

## 現在の状態

- `pcloud-manager` は public Python CLI として稼働中。
- 共有 CLI surface は `help`, `help --ai`, `info`, `status`, `doctor`。
- pushd resident: `com.takafumi.pcloud-pushd` running。`.pcloudmanagerignore` 導入後に reload 済み。
- pushd executor: `com.takafumi.pcloud-pushd-executor` loaded。`--max-records 10`, timeout 3600。
- diffd polling/executor: loaded。bounded download automation validated。
- Current queue: pushd/diffd とも planned/manual-review 0。
- autosync/bisync はこの daemon validation flow では戻さない。`normal sync/resync`, listing cache operations, autosync launchd changes は禁止寄りの別 gate。

## 重要な実装

- `src/pcloud_tools/cli_help.py`: `help --ai`。
- `src/pcloud_tools/cli_status.py`: `info/status/doctor`。
- `src/pcloud_tools/cli_notify.py`, `chat_notify.py`: abnormal notify と dedupe。
- `src/pcloud_tools/download_suppression.py`: download suppression, conflict copy, upload echo suppression.
- `src/pcloud_tools/manager_ignore.py`: `.pcloudmanagerignore` parser。`!` は例外許可。
- `src/pcloud_tools/service_daemon_plan.py`: allowlist/default exclude/manager ignore/hard safety exclude の plan 判定。
- `src/pcloud_tools/cli_service_daemon.py`: pushd/diffd gates, launchd, transfer executor, real-run/automation, queue cleanup。

## 運用メモ

- `.pcloudmanagerignore` は `/Users/takafumi/p-core/.pcloudmanagerignore`。
- `.partial` は ignore 例外では許可しない hard safety exclude。
- `.env` は除外、`.env.sample` は許可。
- Discord notify は abnormal-only。うるさい場合は `pcloud-manager notify disable`。
- `pcloud-manager info paths` で state/config/log/allowlist/ignore paths を再発見できる。
- `pcloud-manager pushd transfer preview` と `diffd transfer preview` は read-only の最初の確認コマンド。

## 検証済み

- full invariant tests: 143 passed.
- shadow validation: ok, `.dev-state/reports/shadow-validation.json` 更新済み。
- live read-only preview: pushd/diffd all zero.
- `git diff --check`: ok.

## 次にやるなら

- 基本はなし。運用観察のみ。
- もし再開するなら、まず `pcloud-manager info`, `pcloud-manager status --detail`, `pcloud-manager pushd status`, `pcloud-manager diffd status` を見る。
- xbar 側に `info paths` や `.pcloudmanagerignore` を表示する必要はない。CLI で十分。

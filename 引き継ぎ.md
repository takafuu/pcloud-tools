# 引き継ぎ

## 次スレ開始手順

1. 最初に読む: `/Users/takafumi/p-core/dev/pcloud-tools/引き継ぎ.md`
2. 次に読む: `/Users/takafumi/p-core/.pcloudmanagerignore`
3. 次に読む: `/Users/takafumi/p-core/dev/pcloud-tools/src/pcloud_tools/service_daemon_plan.py`
4. 次に読む: `/Users/takafumi/p-core/dev/pcloud-tools/src/pcloud_tools/cli_service_daemon/__init__.py`
5. 必要なら読む: `/Users/takafumi/p-core/dev/pcloud-tools/tests/test_pushd.py`
6. 必要なら読む: `/Users/takafumi/p-core/dev/pcloud-tools/tests/test_diffd.py`
7. レビュー履歴が必要なら読む: `/Users/takafumi/p-core/dev/pcloud-tools/報告.md`

## 現在地

- pCloud 運用は `pcloud-manager mode status` 上 `daemon`。
- `pushd` queue は空: pending 0 / uploads 0 / missing 0 / excluded 0。
- `diffd` queue は空: remote 0 / planned downloads 0 / skipped 0。
- bisync/autosync は loaded ではない。`sync --execute` は daemon mode 中に拒否する方針。
- 直近コミット:
  - `727546c Fix recursive manager ignore matching`
  - `df0ecd7 Support root p-core daemon sync scope`
- repo worktree は現時点で未コミット差分あり。remote trash / missing-local cleanup / sync scope 命名整理の変更が同じ作業ツリーに残っている。
- runtime 設定として `/Users/takafumi/p-core/.pcloud-sync-allowlist` は `/`。これは user-facing には sync scope file と呼ぶ。p-core 全体を daemon scope とし、危険物は `.pcloudmanagerignore` / generated filter で落とす。

## 最近の運用変更

- `/Users/takafumi/p-core/.pcloudmanagerignore` を強化済み。
- `Automatic Upload/` は iPhone/pCloud 自動アップロード領域なので pull しない。
- `apps/1Password/`, `*.agilekeychain`, `*.opvault`, `AgileBits` は同期禁止。remote の `pcloud:core/apps/1Password` は削除済み。
- `apps/Local sites/` は Local by Flywheel の実データなので同期禁止。
- `apps/BetterTouchTool/` と `apps/BetterTouchTool.zip` は削除済み。BTT は iCloud Sync を使う。
- TablePlus は `apps/tableplus/settings/**` だけ同期対象。remote には `ConnectionGroups.plist`, `Connections.plist`, `Settings.json` がある。
- `apps/tableplus/*`, `apps/*.plist`, `apps/*.json`, `apps/*.sb-*`, `apps/tableplus.backup-*` は除外。
- zsh 専用バックアップ launchd は削除済み:
  - `com.takafumi.backup-zsh-dotfiles`
  - `com.takafumi.upload-zsh-backups-to-pcloud`
  - local/remote の `bin/backup-zsh-dotfiles.sh`, `bin/upload-zsh-backups-to-pcloud.sh` も削除済み。

## 今回の主要論点

- `Missing local upload records` が削除・移動・リネームで頻出してうるさい。
- 原因: `pushd` は fswatch イベントを upload queue に積む。ファイル移動・rename・atomic save は旧 path の upload 予定が残り、実行時には local source が消えている。
- これは remote delete を意味しない。古い upload queue のゴミとして扱うべき。
- ただし「削除・リネームを全く remote に反映しない」と p-core と pCloud の整合性が崩れる。
- 次の設計目標:
  - create/update は従来通り自動 upload。
  - missing-local は一定時間後に自動 prune し、xbar では強い警告にしない。
  - delete/rename は即時自動反映しないが、遅延レビュー queue に残し、明示 apply で remote delete する。

## 重要判断

- p-core 全体同期の意味は「危険物・生成物・秘密情報を除いた通常ファイルの create/update 同期」。
- `.pcloud-sync-allowlist` は bisync/daemon 共通の sync scope file。実ファイル名と `PCLOUD_TOOLS_ALLOWLIST_FILE` は互換維持のため残すが、CLI/help/docs/status では原則 sync scope と呼ぶ。
- 通常は `.pcloud-sync-allowlist` は `/` の1行で足りる。日常的な除外、例: root 直下の `sensitive-data-dir/` を同期したくない場合は `.pcloudmanagerignore` に書く。
- `.pcloudmanagerignore` と bisync filter に同じ除外思想を反映する。
- 1Password/Local Sites/Automatic Upload/app state は pCloud manager の通常同期対象ではない。
- `delete/rename` は破壊的なので即時実行しない。遅延・レビュー・明示承認が必要。
- `missing-local` は破壊的ではない queue cleanup なので自動化してよい。
- remote trash は top-level の `pcloud-manager trash ...` が主コマンド。旧 `pcloud-manager pushd trash ...` は互換 alias。
- xbar/scraper が status JSON の `actions[]` を読む場合、推奨 ID は `trash.*`。旧 `pushd.trash.*` は action 直接呼び出し alias としては残るが、`actions[]` には出さない。
- remote trash root は既定で `pcloud:core/.pcloud-manager-trash/`。`PCLOUD_TOOLS_REMOTE_TRASH_ROOT` で変更できるが、既存 trash object は旧 root に残るため、初期設定後は基本的に変更しない。
- missing-local upload record は remote delete 扱いしない。pushd executor tick 冒頭で `missing_since` を付与し、TTL (`PCLOUD_TOOLS_PUSHD_MISSING_LOCAL_PRUNE_TTL_SECONDS`, default 600 秒) 経過後だけ自動 prune する。
- fswatch resident と `pushd queue add --execute` が追加する pushd queue record には `enqueued_at` を付ける。既存の string/object queue record は引き続き有効。

## 未完了

- delete/rename 遅延レビュー queue の設計。
  - fswatch の `Removed/Renamed` は既に manual-review action として扱うテストがある。
  - 現状は自動 apply までない。
  - 別 queue か既存 pushd queue の manual-review records を使うか決める。
- `review/apply` コマンド追加。
  - preview: 対象 path、local existence、remote existence、reason、age を表示。
  - apply: 明示 gate + reviewer approval + `--execute` で remote delete。
  - rename は基本「new path upload + old path delete candidate」として扱う。
- tests 追加。
  - delete/rename review queue。
  - remote delete apply の gate/refusal/execute。

## 地雷

- 2026-06-09 時点で `journal/` が remote に無い件を調査。`journal/` は sync scope `/` 内で `.pcloudmanagerignore` でも除外されていない。直接原因は `com.takafumi.pcloud-pushd` が loaded だが `not running` で、last resident run が `returncode=-11` のまま止まっていたこと。旧 operational plist は `KeepAlive=false` なので fswatch resident 終了後に再起動されなかった。
- 修正方針: operational `pushd launchd resident-plist` payload は `KeepAlive=true` にする。`pushd fswatch resident-run` は unbounded mode で fswatch が非ゼロ終了したら error として last-run JSON に `status=failed` を書く。`pushd status` は last resident failed/unknown を warning issue に上げる。
- `journal/` の既存ファイルは backfill preview で upload 対象: `journal/documents/README.md`, `journal/logs/2026-06-01.md`, `journal/logs/2026-06-08.md`。ただし full backfill は 108k 件規模なので一括 enqueue しない。live 復旧は gated plist write/reload を明示確認してから行う。
- live 復旧済み: `PCLOUD_TOOLS_PUSHD_LAUNCHD_RESIDENT_PLIST_GATE` で resident plist を `KeepAlive=true` に書き換え、`PCLOUD_TOOLS_PUSHD_LAUNCHD_RELOAD_GATE` で bootout/bootstrap 済み。`pushd` resident は running。`journal/` 3ファイルは touch で queue 入りし、executor tick で upload/consume 成功。remote `pcloud:core/journal/{documents,logs}` に存在確認済み。
- Claude ラウンド6 review 対応済み。`sync check-allowlist` は argparse alias ではなく parse 前 argv 正規化で legacy 互換にした。理由: Python 3.11.14 では `aliases=` が help に露出するため。`sync --help` は `check-scope` だけ表示する。
- user-facing JSON の `allowlist` 表示漏れは `scope-file` に正規化済み。内部ファイル名 `.pcloud-sync-allowlist` と env 互換は維持。
- pushd resident last-run の `running` は PID 生存確認で stale 判定する。`updated_at` しきい値ではないので、イベントが少ないだけの正常常駐を warning 化しない。
- missing-local cleanup 関数名は `cleanup_stale_missing_local_upload_records` (executor向け合成), `prune_stale_missing_local_upload_records` (TTL prune専用), `force_prune_missing_local_upload_records` (手動即 prune) に整理済み。`annotate_missing_local_upload_records` は `missing_since` 付与専用。
- Claude ラウンド7 review 対応済み。`sync migration-run` の `scope mode` も `scope-file/full` 表示に正規化済み。PID stale 判定は PID 再利用リスクを理論上持つので、将来必要なら `started_at` と macOS `ps -o lstart` の二重照合を検討する。
- `diffd remote-change remove --execute` は本番 state では dev-mode ガードに引っかかる。前スレでは旧スクリプト pull 候補を消すため、`/Users/takafumi/.pcloud/diffd/remote-changes.json` を直接編集した。
- `apps/` 全体を directory ignore すると `apps/tableplus/settings/**` の例外まで探索前に pruning される。今は個別除外にしている。
- `.agilekeychain` は暗号化 vault でも title/meta が見える古い形式。絶対に同期へ戻さない。
- `Automatic Upload/` は iPhone 側の自動アップロード。p-core へ pull しない。
- remote delete/trash apply 実行は高リスク。必ず sync scope, manager ignore, remote existence, local missing, dedicated gate, dry-run を確認する。
- pCloud trash/version に過去の削除済み秘密 copy が残る可能性はある。Web 側掃除は別作業。
- dev `pushd transfer executor-run --execute` は dev-state fake-rclone tick の冒頭 cleanup として missing-local queue cleanup を実行する。public `pushd transfer automation-run --execute` は gate が揃った場合だけ queue cleanup を実行し、refused state では queue を mutate しない。
- xbar/action の `pushd.queue.prune-missing-local` は preview ではなく即 cleanup 実行。missing-local cleanup は非破壊 queue cleanup として扱う設計だが、delete/rename record を消してはいけない。

## 次の一手

1. 現状確認:
   - `/Users/takafumi/p-core/bin/pcloud-manager mode status --json`
   - `/Users/takafumi/p-core/bin/pcloud-manager pushd preview --json`
   - `/Users/takafumi/p-core/bin/pcloud-manager diffd preview --json`
2. 実装調査:
   - `/Users/takafumi/p-core/dev/pcloud-tools/src/pcloud_tools/service_daemon_plan.py`
   - `/Users/takafumi/p-core/dev/pcloud-tools/src/pcloud_tools/cli_service_daemon/__init__.py`
   - `/Users/takafumi/p-core/dev/pcloud-tools/tests/test_pushd.py`
3. delete/rename review/apply flow を小さく実装する。
4. 必要なら trash restore 実行版や trash index rebuild を追加する。
